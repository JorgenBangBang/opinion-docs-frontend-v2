import React from 'react';
import { Box } from '@mui/material';

// Basic placeholder for Logo component
const Logo = () => {
  return (
    <Box sx={{ fontWeight: 'bold', fontSize: '1.5rem' }}>
      LOGO
    </Box>
  );
};

export default Logo;

